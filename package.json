{
  "private": true,
  "workspaces": [
    "back",
    "front"
  ],
  "scripts": {
    "front:start": "yarn --cwd front start",
    "back:start": "yarn --cwd back start",
    "start": "yarn --cwd front start & yarn --cwd back start"
  }
}